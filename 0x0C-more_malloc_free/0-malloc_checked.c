#include "main.h"
#include <stdlib.h>
/**
*malloc_checked - an allocates memory using malloc
*@b: a number of bytes to allocate
*Return: the pointer to the allocated memory
*/


void *malloc_checked(unsigned int b)
{
int main(void);

void *s;
s = malloc(b);
if (s == NULL)
exit(98);
return (s);
}
